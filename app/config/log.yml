monolog:
    handlers:
        main:
            type: stream
            level: 200
            channels: open_orchestra
            handler: mongo
        mongo:
            type: mongo
            level: 200
            mongo:
                host: "localhost"
                database: "%open_orchestra_cms.mongodb.database%"
                collection: log
            channels: [open_orchestra]
